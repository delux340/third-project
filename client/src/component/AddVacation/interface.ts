export interface props {
    actions: { addVacation: Function }

}
export interface state {
    description: string,
    destination: string,
    image: string,
    from: string,
    until: string,
    price: string
}